CoreASIM scheduler

use Standard

init Start

controlled counter:INTEGER

scheduling whileRunning

policy whileRunning = {
	schedule client
	schedule server
	//schedule self
}

rule Start = {
	counter := 0
	program(self) := Program
}

rule Program = {
	counter := counter + 1
	print "scheduler " + counter
}